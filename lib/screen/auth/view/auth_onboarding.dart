import 'package:flutter/material.dart';

class AuthOnboarding extends StatelessWidget {
  const AuthOnboarding({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}